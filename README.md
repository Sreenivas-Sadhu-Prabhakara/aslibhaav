# Aslibhaav — "the real price"

Enter a distributor's scheme (base price, trade discount %, cash discount %,
buy-X-get-Y free goods, freight included/extra) for an item, and see the **true
effective cost per unit** — so opaque schemes become comparable.

Stack: Flutter UI → Go middleware → PostgreSQL. Built via the qcode harness.
